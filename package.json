{
  "devDependencies": {
    "vitepress": "^1.0.0-rc.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev src --host",
    "docs:build": "vitepress build src",
    "docs:preview": "vitepress preview src"
  }
}